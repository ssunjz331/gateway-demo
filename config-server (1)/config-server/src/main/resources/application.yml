server:
  port: 8888


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ssunjz331/spring-cloud-config-repository
#        bootstrap: true
#
#health:
#  config:
#    enabled: false
#management:
#  security:
#    enabled: false


server:
  port: 108888


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ssunjz331/spring-cloud-config-repository

#management:
#  security:
#    enabled: false


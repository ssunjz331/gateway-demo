server:
  port: 8183

spring:
  application:
    name: service-b
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: refresh
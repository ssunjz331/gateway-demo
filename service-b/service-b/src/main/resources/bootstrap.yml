server:
  port: 108182

spring:
  application:
    name: service-b
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:108888

management:
  endpoints:
    web:
      exposure:
        include: '*'
server:
  port: 108181

spring:
  application:
    name: service-a
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:108888


management:
  endpoints:
    web:
      exposure:
        include: '*'
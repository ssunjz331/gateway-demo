server:
  port: 8181

spring:
  application:
    name: service-a
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888


management:
  endpoints:
    web:
      exposure:
        include: refresh, env